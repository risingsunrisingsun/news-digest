# 봇 토큰. 클라우드에서는 GitHub Secrets 가 대신한다.
.env

# 실행 로그는 로컬 전용.
*.log

# 클라우드 분업 과정에서 생기는 중간 파일 (루틴이 /tmp 에 쓰지만 혹시 몰라서).
candidates.json
summaries.json

# archive.json / state.json / pending.json 은 더 이상 무시하지 않는다.
# 클라우드 에이전트는 빈 샌드박스에서 시작하므로, 이 세 파일이 저장소에
# 있어야 수집분·게시 이력·승인 대기 초안이 실행 간에 이어진다.
